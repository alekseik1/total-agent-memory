ARG DEV_IMAGE=tam-development:14.1.0
FROM ${DEV_IMAGE}
ARG TORCH_INDEX_URL
RUN test -n "$TORCH_INDEX_URL" && python -m pip install --no-cache-dir torch --index-url "$TORCH_INDEX_URL"
RUN python -m pip install --no-cache-dir 'sentence-transformers>=5,<6'
